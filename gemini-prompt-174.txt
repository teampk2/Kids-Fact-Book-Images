Issue: "Remove Inner White Parts" — Same Bug Repeated Without Fix
Location:
"Single Preset Renderer" → "Remove Inner White Parts" option.

Status:
❌ Repeatedly reported — still NOT fixed.

Problem Statement:
This is the same problem repeating continually without any rectification or fix. When the user enables the "Remove Inner White Parts" option and uploads an SVG file, the entire image turns black, and the layer shapes also become black — instead of only removing the targeted internal white parts.

Instruction to Developer:
Use your mind. Take this seriously. The same bug has been reported multiple times and remains unresolved. If you do not already know the fix, search the entire internet/web to find the correct solution and implement it properly. Do not return another incomplete fix. The issue must be fully resolved this time.

Correct Behavior Required:

Only the specific internal white parts and elements (or other specifically targeted colors) should be removed — becoming transparent or eliminated.

The entire image must NEVER turn black.

Layer shapes must NEVER turn black.

All other elements, layers, shapes, and colors must remain fully intact and unchanged.

The fix must work correctly both independently and alongside the existing "Remove Background" option.

Reference:
Attached screenshots show the original uploaded image and the result image after processing — clearly showing the entire image and layer shapes turning black.
