1). **Theme-Based Heading Colors**

Currently, all headings and sub-headings throughout the book use the same color. Update the logic so that **all heading text automatically uses colors that match the overall theme of the book** instead of a single default color.

### Requirements

* Apply a consistent, theme-based color palette to all headings.
* Automatically select attractive, high-contrast colors that complement the book's theme and design.
* Maintain visual consistency across every page and section.
* Ensure all heading colors remain easy to read on both screen and in print.
* Preserve the existing font, size, alignment, spacing, and layout—only change the heading color.
* Apply the theme colors uniformly to all current books and automatically to all future books.

2). **Preserve PNG/SVG Transparency in the 31 Grid Image PDF Output**

In the **Kids Facts Book App → 31 Grid Image** section, when the user uploads **PNG** or **SVG** images with transparent backgrounds, the transparency is currently being replaced with a **white background** in the generated PDF. This is incorrect.

### Required Fix

* Preserve the original transparency of all uploaded **PNG** and **SVG** images throughout the entire workflow.
* The images displayed in the **live preview** and the **generated PDF** must exactly match the original uploaded files.
* Do **not** flatten transparent images onto a white background or any other background during PDF generation.
* Render PNG alpha channels and SVG transparency correctly in the PDF while preserving fills, strokes, opacity, gradients, and anti-aliasing.
* Ensure transparent areas remain transparent and only the actual artwork is visible.
* Maintain the original image quality, sharpness, colors, proportions, and resolution without adding blur, compression, or artifacts.
* Use a PDF rendering method that fully supports PNG alpha transparency and native SVG transparency instead of rasterizing or flattening the images.
* Apply this fix to **all 31 grid images**, the **preview**, the **PDF output**, and all **future books** automatically.

