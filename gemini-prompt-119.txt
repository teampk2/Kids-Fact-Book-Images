8.25" × 11" Hardcover Creator (Black & White & Color) – Implement KDP Standard Logic

In the **Popular Interiors** section, update both hardcover generators to follow the official KDP specifications exactly. The **8.25" × 11" Black & White Hardcover Creator** and **8.25" × 11" Color Hardcover Creator** must be handled independently because their spine widths and overall cover dimensions differ.

### General Requirements

* Set the **default page count to 238 pages** in both tools.
* The page count should remain editable by the user, but **238** must be the default value.
* Do **not** use one calculation for both hardcover types.
* The **Black & White Hardcover** and **Color Hardcover** generators must each calculate their own dimensions based on:

  * Interior type
  * Paper type
  * Trim size
  * Page count
  * KDP hardcover specifications
* Every calculation must match the official KDP Cover Calculator exactly.

---

## 1. 8.25" × 11" Black & White Hardcover Creator

Use the blueprint shown in the attached Black & White reference image.

Implement and calculate automatically:

* Full Cover Width
* Full Cover Height
* Front Cover Width
* Front Cover Height
* Back Cover Width
* Back Cover Height
* Trim Size
* Safety Margin
* Wrap (Bleed)
* Hinge Width
* Spine Width
* Spine Safe Area
* Spine Margin
* Barcode Safe Area
* Left and Right Board Extension
* Hardcover Wrap Fold
* All hardcover offsets
* PDF canvas size
* Export size
* Safe guides
* Bleed guides
* Trim guides

Every value must update automatically whenever:

* Page count changes
* Trim size changes
* Interior type changes
* Paper type changes

---

## 2. 8.25" × 11" Color Hardcover Creator

Use the blueprint shown in the attached Color reference image.

Implement the same calculations as above, but calculate everything independently for the **Color Hardcover** version.

Because the paper thickness differs, calculate separately:

* Spine Width
* Full Cover Width
* Spine Safe Area
* Cover Layout

Never reuse the Black & White values.

---

## Dynamic KDP Calculation Engine

Create a dedicated hardcover calculation engine that automatically computes:

* Full Cover Width
* Full Cover Height
* Front Cover
* Back Cover
* Spine Width
* Spine Safe Area
* Hinge
* Wrap
* Bleed
* Safety Margins
* Barcode Area
* Hardcover Board Extensions

using the selected:

* Trim Size
* Interior Type
* Paper Type
* Page Count
* Reading Direction
* Binding Type

Every calculation must update instantly whenever any input changes.

---

## PDF & Cover Generation

When generating the final PDF:

* Use the calculated dimensions exactly.
* The PDF canvas must match the calculated full cover size precisely.
* Do not scale or stretch artwork after calculation.
* Position the front cover, spine, and back cover using the calculated values only.
* Maintain exact bleed, trim, hinge, wrap, spine, and safety areas.
* Embed all fonts.
* Export at **300 DPI** with print-ready quality.

---

## Validation Before Export

Before exporting the cover:

* Validate every calculated dimension.
* Verify that the generated PDF exactly matches the calculated full cover size.
* Verify spine placement.
* Verify bleed.
* Verify wrap.
* Verify hinge.
* Verify barcode safe area.
* Verify trim boundaries.
* Prevent export if any calculation does not match the official KDP specifications.

---

## Expected Result

The **8.25" × 11" Black & White Hardcover Creator** and **8.25" × 11" Color Hardcover Creator** must function as two independent KDP-standard cover generators. Each tool must calculate its own dimensions accurately according to the selected interior specifications and produce a **100% KDP-compliant print-ready PDF** that uploads to Amazon KDP without any cover size, spine, bleed, margin, or template validation errors.
